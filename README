Build the program:
    VTK_DIR=LOCATION_TO_VTK cmake -H. -Bbuild
    cd build && make && make install && cd ..

Run the program (OMEGALIB HAS BEEN INSTALLED)
    bin/splitToolkit <CONFIG_FILE>

Please check SplitTool/task1sample1m1.cpp. 
   
